[workspace]
members = [
    "crates/monitor-agent",
    "crates/monitor-common",
    "crates/monitor-server",
]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT"
rust-version = "1.88"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
